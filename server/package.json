{
  "name": "sendfilesfree-signaling",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight WebRTC signaling server for SendFilesFree. Only brokers offer/answer/ICE messages between a sender and receiver paired by a short room code — never sees file bytes.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node test-signaling.mjs"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
